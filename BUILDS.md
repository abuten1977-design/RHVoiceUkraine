# Збірки та вихідний код / Builds and Corresponding Source

Кожна збірка, роздана через TestFlight, позначена тегом у цьому репозиторії.
Тег `build-N` вказує на коміт, з якого зібрано збірку номер N.

**Як отримати вихідний код конкретної збірки.** Номер збірки видно в застосунку:
екран «Ліцензії» → розділ «Правова інформація». Далі:

```sh
git clone https://github.com/abuten1977-design/RHVoiceUkraine.git
cd RHVoiceUkraine
git checkout build-213     # замініть на потрібний номер
```

**Як зібрати.** Потрібні macOS з Xcode і XcodeGen:

```sh
cd UkrainianVoicesApp
xcodegen generate
xcodebuild -project UkrainianVoices.xcodeproj -scheme UkrainianVoices \
           -destination 'generic/platform=iOS' build
```

Для версії під macOS використовується схема `UkrainianVoicesMac`.

## Перелік збірок (iOS)

| Збірка | Тег | Що з'явилося |
|---|---|---|
| 215 | `build-215` | НЕ роздана тестерам. Зібрана помилково: за кодом дорівнює 214 (різниця лише в текстових файлах) |
| 214 | `build-214` | Діагностика: показ тексту, який VoiceOver передає синтезатору |
| 213 | `build-213` | Тексти ліцензій справді потрапляють у застосунок |
| 212 | `build-212` | GPL-3 з App Store exception, перероблений екран «Ліцензії» |
| 211 | `build-211` | Надійна доставка змін стану у VoiceOver |
| 210 | `build-210` | Доступність форм і дій для VoiceOver |
| 209 | `build-209` | Доступність словника, налаштування прослуховування |
| 208 | `build-208` | Власні записи словника замін доходять до голосу |
| 207 | `build-207` | Кешований пошук по словнику — прибрано гальмування |
| 206 | `build-206` | Обмін словниками замін (експорт та імпорт) |
| 205 | `build-205` | Словник замін: базовий список і власні записи |
| 204 | `build-204` | Суми з розділювачем тисяч у звичайному тексті |
| 203 | `build-203` | Людські імена голосів, самоперевірка, кнопка «Полагодити голоси» |
| 202 | `build-202` | Без вигаданої валюти; номер картки по цифрах |
| 201 | `build-201` | Телефони групами чисел; суми проти номерів |
| 197 | `build-197` | Прибрано дублювання знака «плюс» |
| 196 | `build-196` | Профіль голосу при синтезі; розгортання «хв», «год», «сек» |
| 195 | `build-195` | Перемикач «Читати час словами» |
| 194 | `build-194` | Опублікований каталог голосів — голоси видно у VoiceOver |
| 193 | `build-193` | Спроба виправити пошук завантажених голосів |
| 192 | `build-192` | Завантажені голоси на головному екрані та в системі |
| 191 | `build-191` | Завантажувані англійські голоси |
| 190 | `build-190` | Дати з екрана блокування, час словами, латинські абревіатури |

Збірки 198 та 200 не мають тегів: 198 — технічна, 200 — окремий експеримент із
іншою версією рушія, не з цієї гілки.

## Збірки для macOS

| Збірка | Що з'явилося |
|---|---|
| 2 | Новий дисклеймер донора + екран «Що почув синтезатор» (відповідає iOS-збірці 214) |
| 1 | Перша macOS-збірка у TestFlight (липень 2026) |

macOS-версія збирається з тієї самої гілки й тих самих файлів, що й iOS: екрани
`ContentView.swift` спільні. Номери збірок macOS і iOS не пов'язані — це окремий
запис у App Store Connect.

## Правило на майбутнє

Кожна нова збірка, яка йде в TestFlight або App Store, спершу комітиться, потім
позначається тегом `build-N`, і лише тоді збирається. Збірка з незбережених змін
не роздається — інакше неможливо видати вихідний код саме тієї версії, як того
вимагає GPL-3.
